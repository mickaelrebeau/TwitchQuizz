# URL du serveur (Socket.IO + API, ex. statut Twitch GET /api/twitch/status).
# En dev : souvent http://localhost:3000 (port du serveur).
# En prod : laisser vide pour utiliser la même origine que la page (window.location.origin).
VITE_SOCKET_URL=http://localhost:3000
